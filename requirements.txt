# Federated Learning
flwr>=1.8

# Computer Vision / Deep Learning
torch>=2.2
torchvision>=0.17
monai>=1.3

# Privacy & Encryption
tenseal>=0.3.14

# gRPC / networking
grpcio>=1.62
grpcio-tools>=1.62

# Utilities
numpy>=1.26
nibabel>=5.2
scikit-learn>=1.4
websockets>=12.0

# Module 3: raw BraTS dataset pipeline
matplotlib>=3.8   # cv_model/brats/inspect_slices.py only -- MONAI's own slice-plot
                   # utility (monai.visualize.matshow3d) requires it as a backend too
pytest>=8.0        # cv_model/brats/tests/

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fedmed"
version = "0.1.0"
description = "FedMed cross-silo federated learning engine for privacy-preserving MRI tumor segmentation"
requires-python = ">=3.10"
dependencies = [
    "flwr>=1.13",
    "torch>=2.2",
    "torchvision>=0.17",
    "monai>=1.3",
    "numpy>=1.26",
]

[tool.hatch.build.targets.wheel]
packages = ["cv_model", "hospital_nodes", "server"]

[tool.flwr.app]
publisher = "fedmed"

[tool.flwr.app.components]
serverapp = "server.server_app:app"
clientapp = "hospital_nodes.client_app:app"

[tool.flwr.app.config]
num-server-rounds = 3
local-epochs = 1
local-val-fraction = 0.0


# Module 8: a real, TLS-secured local SuperLink deployment (see
# docs/secure_communication.md). `insecure` is intentionally left unset -- Flower
# defaults to requiring TLS, matching flower-superlink's own "HTTPS enabled by default"
# behavior. `root-certificates` is NOT set here: Flower requires it to be an absolute
# path, which is machine-specific and must not be hard-coded into a committed file --
# pass it at invocation time instead:
#   flwr run . local-deployment --federation-config 'root-certificates="<absolute path to certs\ca.crt>"'

#######################################################################
# CONFIGURATION MIGRATION NOTICE:
#
# What was previously called "federation config" for SuperLink
# connections in pyproject.toml has been renamed and moved.
#
# These settings are now **SuperLink connection configuration**
# and are defined in the Flower configuration file.
#
# The entries below are commented out intentionally and are kept
# only as a migration reference.
#
# Docs: https://flower.ai/docs/framework/ref-flower-configuration.html
#######################################################################

# [tool.flwr.federations]
# default = "local-deployment"

# [tool.flwr.federations.local-deployment]
# address = "127.0.0.1:9093"

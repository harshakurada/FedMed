{
  "name": "fedmed-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "FedMed training dashboard — visualizes global model convergence, accuracy metrics, and segmentation masks across federated hospital nodes.",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.12.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test"
  }
}

"""FedMed encryption package: planned TenSEAL homomorphic-encryption boundary.

Nothing in this package performs real encryption yet (Week 3). Keeping a
dedicated package now means future ciphertext/key logic has one place to
live instead of leaking into `server/` or `hospital_nodes/`.
"""
